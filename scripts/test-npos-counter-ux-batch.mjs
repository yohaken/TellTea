import assert from "node:assert/strict";
import { readFileSync, existsSync } from "node:fs";
import { dirname, join } from "node:path";
import { fileURLToPath } from "node:url";
const root = join(dirname(fileURLToPath(import.meta.url)), "..");
const read = (p) => readFileSync(join(root, p), "utf8");
assert.match(read("src/lib/version.ts"), /APP_BUILD = 331/);
assert.match(read("src/lib/pos-version.ts"), /POS_BUILD = 119/);
assert.match(read("npos-telltea/app/build.gradle"), /versionCode\s+90/);
assert.match(read("npos-telltea/app/build.gradle"), /versionName\s+"1.14.67"/);
assert.ok(existsSync(join(root, "docs/npos-counter-ux-batch-checklist.md")));
const sell = read("npos-telltea/app/src/main/java/app/telltea/npos/SellActivity.java");
assert.match(sell, /npos_menu_qty_badge/);
assert.match(sell, /dutyTick|dutyLine/);
assert.match(sell, /pay_pp_hidden_early|GONE/);
assert.doesNotMatch(sell, /QrBitmaps\.encode/);
assert.match(read("npos-telltea/app/src/main/java/app/telltea/npos/shift/ShiftPrefs.java"), /dutyLine/);
assert.match(read("functions/npos-sell.js"), /brandLogo/);
assert.match(read("npos-telltea/app/src/main/java/app/telltea/npos/shell/PosShellNav.java"), /applyBrandLogo/);
assert.match(read("npos-telltea/app/src/main/res/layout/activity_sell.xml"), /payPromptButton[\s\S]*android:visibility="gone"/);
console.log("OK test-npos-counter-ux-batch");
