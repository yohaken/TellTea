# nPos — เตะในตาราง · เปลี่ยนรหัสแล้วเด้งล็อกใหม่

อัปเดต: **1.14.36** · อ้างอิงหลัง `1.14.31` (local-first claim)  
เป้าหมาย: หลังบ้านเตะจากตารางได้ชัด · เปลี่ยนรหัสร้านแล้วแท็บเล็ตถูกเตะอัตโนมัติ · อัป hash ในเครื่อง · ต้องใส่รหัสใหม่

อ้างอิง: `NposDevicesPanel` · `PosStoreClaimPanel` · `nposOwnerDeviceCommand` · `StoreClaimPrefs` / heartbeat · `NposApp` kick bounce

---

## นโยบายที่ล็อก

| หัวข้อ | ตัดสิน |
|--------|--------|
| ปุ่มเตะ | อยู่ใน **ตาราง slim** ทุกแถวที่ถือ seat / เคลมอยู่ (เด่น อ่านง่ายบนมือถือ) |
| เตะ | เคลียร์ seat + `storeClaimed: false` · เหตุ `kicked` · **ไม่ปิดกะ** |
| เปลี่ยนรหัสร้าน | = เตะทุกเครื่องอัตโนมัติ · เหตุ `code_changed` · seat ว่าง |
| ฝั่งแท็บเล็ต | heartbeat/shop เห็น hash ใหม่หรือถูก revoke → **เคลียร์ claim local** · อัป hash · เด้งหน้าใส่รหัส |
| กะ | ไม่ตัดตอนเตะ / เปลี่ยนรหัส (ตาม exclusive seat S3) |

---

## K1 — ปุ่มเตะในตาราง (BO)

- [x] K1.1 ตาราง slim เปิดเป็นค่าเริ่มต้นบน `/pos-sales/?tab=manage`
- [x] K1.2 คอลัมน์แอ็กชันมีปุ่ม **เตะ** ชัด (ไม่ซ่อนในเมนูย่อย)
- [x] K1.3 โชว์เตะเมื่อ: `storeClaimed` **หรือ** เป็น `activeSeatInstallId` (แม้สถานะเพี้ยน)
- [x] K1.4 ยืนยันสั้น: «เตะแล้วเครื่องเด้งใส่รหัส · กะไม่ปิด»
- [x] K1.5 หลังเตะ: แถวเป็น «ถูกเตะ» / seat ว่าง · รีเฟรชตารางทันที
- [x] K1.6 (เสริม) ปุ่ม **เคลียร์ seat ทั้งหมด** ที่หัวตารางหรือแผงรหัส — สำรองเมื่อไม่รู้แถวไหน

**จบ K1 เมื่อ:** เจ้าของมือถือเห็นปุ่มเตะในตารางแล้วกดได้จริง

---

## K2 — เปลี่ยนรหัส = เตะอัตโนมัติ (เซิร์ฟเวอร์)

- [x] K2.1 `set_store_code` เคลียร์ `activeSeatInstallId`
- [x] K2.2 revoke ทุกเครื่องที่ `storeClaimed` · เหตุ `code_changed`
- [x] K2.3 ตอบกลับ BO: จำนวนเครื่องที่เตะ + hint รหัสใหม่
- [x] K2.4 Gate assert: เปลี่ยนรหัสแล้วไม่มี seat / ไม่มี claimed ค้าง (`test-npos-kick-reclaim.mjs`)

**จบ K2 เมื่อ:** หลังบ้านเปลี่ยนรหัสครั้งเดียว → seat ว่าง + ทุกเครื่อง revoked

---

## K3 — ลูกค้า (แท็บเล็ต) อัป hash + บังคับล็อกใหม่

- [x] K3.1 heartbeat ส่ง `storeClaimCodeHash` + `storeClaimUpdatedAt` (มีแล้วบางส่วน)
- [x] K3.2 ถ้า hash ในเครื่อง **ต่างจาก** เซิร์ฟเวอร์ → อัป cache hash ทันที
- [x] K3.3 ถ้าเคยถือ seat / claimed อยู่ตอน hash เปลี่ยน → `clearClaim` + เด้งหน้าใส่รหัส (toast: รหัสร้านเปลี่ยน)
- [x] K3.4 ถ้า `kicked` / `code_changed` จาก heartbeat → เหมือนถูกเตะ (**NposApp** ปิดหน้าขาย/ตั้งค่า/กะ แล้ว CLEAR_TOP กลับหน้าใส่รหัส · heartbeat ~10s)
- [x] K3.5 local-first เคลมด้วย hash เก่าไม่ได้หลังอัปแล้ว
- [ ] K3.6 Gate + คนเทส: เปลี่ยนรหัสบนมือถือ → แท็บเล็ตเด้งใส่รหัสโดยไม่ต้องรีสตาร์ทแอป (รอ heartbeat ≤ ~15 วิ หรือ force)

**จบ K3 เมื่อ:** เปลี่ยนรหัสแล้วเครื่องที่ล็อกอยู่เด้งใส่รหัสใหม่ + ใช้รหัสใหม่เข้าได้

---

## คนเทส

- [ ] E1 ตารางมีปุ่มเตะ · กดแล้วเครื่องเด้งใส่รหัส · ขายไม่ได้
- [ ] E2 เตะแล้วกะยังเปิดบนเซิร์ฟเวอร์ (ไม่ตัด Z)
- [ ] E3 ตั้ง/เปลี่ยนรหัสร้าน → เครื่องเก่าเด้ง · hash ในเครื่องอัป
- [ ] E4 ใส่รหัสใหม่ → เคลมได้เครื่องเดียว
- [ ] E5 เครื่องสองใส่รหัสใหม่ตอน seat ถูกจอง → seat_taken จนกว่าจะเตะ

---

## ลำดับทำ

```
K1 ปุ่มเตะในตารางให้ชัด  →  K2 ยืนยัน/เกตเปลี่ยนรหัส  →  K3 ลูกค้าเด้ง+อัป hash
```

| เฟส | กระทบ | ความเสี่ยง |
|-----|--------|------------|
| K1 | `NposDevicesPanel` | ต่ำ |
| K2 | `npos-owner-device` (ส่วนใหญ่มีแล้ว) | ต่ำ |
| K3 | heartbeat prefs + MainActivity | กลาง — อย่าเด้งหลอกตอน hash ยังไม่เคยมีในเครื่อง |

---

## ตรวจ

```bash
SKIP_CAPTURE_SMOKE=1 node scripts/check-npos-shop.mjs
node scripts/test-npos-kick-reclaim.mjs
node scripts/test-npos-exclusive-seat.mjs
```

แต่ละเฟสโค้ด: bump `APP_BUILD` / `POS_BUILD` / nPos version · ship ผ่าน `main`
