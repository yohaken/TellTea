# nPos ↔ Web — เทียบช็อตต่อช็อต (แคป / จอลูกค้า / แถบซ้าย / อัปเดต)

อัปเดต: **1.14.7** · `APP_BUILD` 237

เป้า: แปลง UI เว็บ (`PosAppShell` + `PosSellView` + `PosOptionPickerModal`) เป็น native เป๊ะ ๆ — ไม่ออกแบบใหม่ · แคปจอลูกค้าเต็มละเอียด · อัปเดตไม่บังคับเข้าตั้งค่า

## ตารางเทียบ

| # | ช็อต / พฤติกรรม | เว็บ | Native ก่อน | Native เป้า (1.14.7) | สถานะ |
|---|----------------|------|-------------|----------------------|--------|
| **P1** | แคปจอลูกค้า = UI จริง เต็มละเอียด | — | แคป **probe** ทับจอ | PixelCopy จาก `CustomerDisplayPresentation` ที่โชว์อยู่ · fallback probe | [x] |
| **P2** | สไลด์โปรโมจอลูกค้า | — | หมุน ~4.5 วิ | **5 วิ** คงที่ | [x] |
| **P3** | แถบเครื่องมือเมนูซ้ายตลอด | `.pos-sidebar` `#2a3038` · active `#2d7fe0` · 11rem | ฮับเป็นปุ่มแนวตั้ง · ขายไม่มีแถบ | `include_pos_sidebar` บนหน้าขาย · ป้าย/ลำดับตาม `pos-nav.ts` | [x] |
| **P4** | ผังขาย หมวด+กริด+ตะกร้า | `.pos-sell-layout` | 65/35 + ปุ่มส้ม | คง 65/35 · ตัดแถบล่างซ้ำ (ย้ายเข้าซ้าย) | [x] |
| **P5** | ปุ่มสั่งกาแฟ / ตัวเลือก | `PosOptionPickerModal` hero+qty+ชิป | `AlertDialog` radio/checkbox | Dialog hero รูป · qty · กลุ่มตัวเลือก · ปุ่มส้มใส่ตะกร้า | [x] |
| **P6** | สีปุ่มชำระ | ส้ม `#e85d24` | `#E85D24` | คง | [x] |
| **P7** | มีอัปเดต — ไม่รอตั้งค่า | (เว็บ watcher) | แบนเนอร์ใน Settings เท่านั้น | Popup มุมซ้ายบนบนขาย/ฮับ · กดอัปเดตทันที | [x] |
| **P8** | หลังอัปเดตกลับงานเดิม | — | เปิดแอปใหม่ที่หน้า clock/hub | จำรอบเปิดอยู่ → เปิดหน้าขายใหม่ · พักบิลอัตโนมัติถ้ามีตะกร้า | [x] |

## ตรวจอัตโนมัติ
```bash
node scripts/test-npos-web-parity-shot.mjs
SKIP_CAPTURE_SMOKE=1 node scripts/check-npos-shop.mjs
```

## มือ (emu)
| # | ผ่านเมื่อ |
|---|-----------|
| 1 | สั่งแคปจาก BO → รูปจอสอง = โปรโม/ตะกร้าจริง ไม่ใช่ข้อความแคปทดสอบ |
| 2 | จอลูกค้า Idle เปลี่ยนรูปทุก ~5 วิ |
| 3 | หน้าขายมีแถบซ้ายสีเทาเข้ม · ปุ่ม active ฟ้า · ลำดับเหมือนเว็บ |
| 4 | จิ้มเมนูมีตัวเลือก → modal มีรูป/จำนวน/ปุ่มส้ม |
| 5 | มีอัปเดต → popup ซ้ายบน · กดแล้วติดตั้ง · กลับหน้าขายโดยไม่ปิดรอบ |

เศษงานถัดไป: W6 สื่อโปรโมจาก BO · qty-per-option ละเอียดเท่าเว็บทุกกลุ่ม · S3/P4 นำร่องร้าน
