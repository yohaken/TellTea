# nPos — Gpos chrome บนเคาน์เตอร์ (กริดฮับ + ตะกร้า slim)

อัปเดต: **1.14.54** · `APP_BUILD` 312 · `POS_BUILD` 107 · vc **77**

## มติ (ยืนยันแล้ว)

1. **ซ่อนเมนูเข้ากริดซ้ายบน** — ตั้งค่า / รอบ / ประวัติ / บิลค้าง / X / ปิดรอบ / กลับหลัก อยู่ใน PopupMenu  
2. **ตะกร้าขวาแถวบาง** — ชื่อ · − / qty / + · ราคา (qty→0 = ลบ)  
3. หัวจอ: กริด + แบรนด์ + ค้นหาเมนู · แถบซ้ายบนหน้าขายซ่อน (หน้าอื่นยังมีแถบ)

พฤติกรรมขาย (จ่าย / พักบิล / ส่วนลด / ตัวเลือก) ไม่เปลี่ยน

## ตรวจ

```bash
node scripts/test-npos-gpos-chrome.mjs
```
