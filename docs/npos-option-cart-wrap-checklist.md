# nPos — option jump + cart wrap (tea-prep)

อัปเดต: **1.14.58** · `APP_BUILD` 316 · `POS_BUILD` 111 · vc **81**

## สเปก
- [x] กด **ตกลง** แล้วยังไม่ครบกลุ่มบังคับ → เลื่อนไปกลุ่มนั้นทันที (ไม่ smooth)
- [x] ตะกร้า: ชื่อเมนูขนาดใกล้กริดซ้าย · **ห้าม ellipsis** · ตัดขึ้นบรรทัดใหม่ได้
- [x] ออปชันเรียงแนวตั้งทีละบรรทัด `- ชื่อ xN` (ไม่เรียง · ในบรรทัดเดียว)

```bash
node scripts/test-npos-option-cart-wrap.mjs
```
