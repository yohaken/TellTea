# nPos — จัดการเมนู UX (จอเตี้ย / สื่อสารไกล)

อัปเดต: **1.14.105** · vc **126** · `APP_BUILD` 546 · `POS_BUILD` 158

## เฟส

| เฟส | งาน | สถานะ |
|-----|-----|--------|
| **P1** | แถวปุ่มหมวดห่อ `HorizontalScrollView` — ไม่ล้นขอบจอ | ทำ |
| **P2** | ค้นหาเล็กในแถบเครื่องมือ (~% ความกว้าง) — กรองชื่อเมนู/หมวด | ทำ |
| **P3** | ปุ่มของหมดแยกชัด · คำสั้น「ของหมด」/「เปิดขาย」 | ทำ |
| **P4** | หน้าแก้เมนู:「บันทึก」ติดล่างจอ (sticky) · ฟอร์มเลื่อนได้ | ทำ |
| **P5** | Gate + bump APK + deploy | ทำ |

## หลักการสเกล
ใช้สัดส่วน / `weight` / scroll — ไม่ล็อกพิกเซลตายตัวทั้งจอ (จออนาคตเปลี่ยนได้)

## นอกขอบเขตรอบนี้
เรียงลำดับเมนูในหมวด · เปลี่ยนโครงแท็บ · RBAC

```bash
node scripts/test-npos-menu-admin-ux.mjs
node scripts/test-npos-menu-admin.mjs
```
