# nPos — counter UX batch (1.14.28)

อัปเดต: **1.14.29** · `APP_BUILD` 287 · `POS_BUILD` 82 · `versionCode` 52

## ทำแล้ว
- [x] แบดจ์จำนวนบนการ์ดเมนู (วงกลมมุมขวาบน) · รีเฟรชตอนแก้ตะกร้า
- [x] แถบรอบงาน: เข้างาน HH:mm · ผ่านมา H:MM:SS เรียลไทม์ + สรุปยอด
- [x] ซ่อนปุ่ม PromptPay / ไม่โชว์ QR บนขาย + จอลูกค้า (ช่วงแรก)
- [x] โลโก้หลังร้าน (`meta/brandLogo`) → แถบซ้าย + task description
- [x] เคลมบน emulator: ข้อความชัดเมื่อยังไม่ตั้งรหัส / rejectDev

## ทำไมเครื่องจำลองไม่ถามรหัส
1. หลังบ้านยังไม่ตั้งรหัสร้าน → `storeClaimRequired=false` → ไม่โชว์ช่องรหัส
2. หรือเคลมแล้ว / เปิดกะอยู่ (ช่องรหัสอยู่หน้าเข้างานเท่านั้น)
3. ถ้าตั้งรหัสแล้วแต่ติ๊กปิดกั้นเครื่องจำลอง → เห็นข้อความบล็อก ไม่ใช่ช่องกรอกหลังเคลมไม่ผ่าน

## ไอคอนบนโฮมสกรีน
Android ไม่ให้เปลี่ยน `ic_launcher` จาก URL ตอนรัน — ใช้โลโก้ในแอป (แถบซ้าย) + ไอคอนใน recent apps; mipmap ยังเป็นค่า build
