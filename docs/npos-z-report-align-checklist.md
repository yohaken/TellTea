# nPos — จัดคอลัมน์สลิปปิดรอบ + เช็คลิสก่อนเซ็น

อัปเดต: **1.14.82** · `APP_BUILD` 409 · `POS_BUILD` 134 · `versionCode` 105

เป้าหมาย: ตัวเลขบนสลิป X/Z อ่านเป็นระเบียบ — **จำนวนบิลไม่ติดกับยอดเงิน**  
และมีเช็คลิสตรวจก่อนเซ็น / ส่งเงิน

อ้างอิงเคสจริง: บิลเงินสด 8 / ยอด 301 อ่านผิดเป็น 8,301 เพราะคอลัมน์ชิด

---

## สาเหตุ

| ชั้น | ปัญหา |
|------|--------|
| Native ESC/POS | `tripleRow` เคยต่อ mid+right ด้วยช่องว่างเดียว (`8` + `301` → `8 301`) |
| เว็บ HTML | ตาราง `.pay` ไม่ล็อกความกว้าง · ไม่มี padding แนวนอน |

---

## ทำแล้ว

- [x] Native `tripleRow` — คอลัมน์ mid/ยอดความกว้างคงที่ + pad ซ้าย + ช่องว่าง 2 ตัว
- [x] หัวตารางหมวด / รายการ (`หมวด|จำนวน|ยอด`, `รายการ|จำนวน|ยอด`)
- [x] เว็บ `.pay` — `table-layout: fixed` · กว้าง 52/14/34 · padding คั่นคอลัมน์ · `tabular-nums`
- [x] เช็คลิส Z ก่อนเซ็น / ส่งเงิน (native + เว็บ):
  - [ ] นับรวมเงินทอนเริ่มต้นแล้ว
  - [ ] ยอดที่ต้องนำส่งตรงกับเงินในมือ
  - [ ] โอน/PromptPay ตรวจสลิปแล้ว
  - [ ] ส่วนต่างมีเหตุผล (ถ้ามี)
- [x] เกต `scripts/test-npos-z-report-align.mjs`
- [x] ใส่ใน `check-npos-shop.mjs`

## คนเทสเคาน์เตอร์

- [ ] พิมพ์ Z จาก EMP หลังอัปเดต 1.14.72
- [ ] แถวเงินสด: คอลัมน์บิลกับยอดแยกชัด (เคส 8 บิล / 301 บาท)
- [ ] แถวโอน / สุทธิ จัดแนวขวาตรงกัน
- [ ] มีบล็อก «ตรวจก่อนเซ็น» ก่อนลงชื่อ
- [ ] Snapshot (X) ไม่มีเช็คลิส/เซ็น

---

## ไฟล์หลัก

| ไฟล์ | บทบาท |
|------|--------|
| `.../printer/ShiftReportFormBuilder.java` | ESC/POS คอลัมน์ + เช็คลิส |
| `src/lib/pos-printer/shift-snapshot-template.ts` | ใบพิมพ์เว็บ |
| `docs/npos-z-report-align-checklist.md` | เช็คลิสต์นี้ |

---

## ตรวจ

```bash
node scripts/test-npos-z-report-align.mjs
node scripts/test-npos-z-report-form.mjs
node scripts/test-npos-z-web-form-parity.mjs
```
